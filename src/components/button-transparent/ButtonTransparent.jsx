import { StyledButtonTransparent } from './styled';

const ButtonTransparent = ({ text, color }) => {
	return (
		<StyledButtonTransparent color={color}>{text}</StyledButtonTransparent>
	);
};
export default ButtonTransparent;
